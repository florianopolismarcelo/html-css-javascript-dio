const prompt = require("prompt-sync")({ sigint: true });

const nome = "Safira Marcelo Alexandre"
for (let i = 0; i < nome.length; i++) {
  console.log(nome[i]);
}

/*
abaixo é um exemplo com a atribuição da variável letra

const nome = "Safira Marcelo Alexandre";
for (let i = 0; i < nome.length; i++) {
  const letra = nome[i]
  console.log(letra);
}
*/
