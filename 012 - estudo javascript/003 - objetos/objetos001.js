const prompt = require("prompt-sync")({ sigint: true });

const safira = {
  nome: 'Safira a gatinha',
  idade: 2 
};

console.log(`${safira.nome}`)
console.log(`${safira.idade}`)
console.log(safira)


 
